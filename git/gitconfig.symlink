[user]
	name = Brian Batten
	email = bjbatten@gmail.com
[color]
  diff = auto
  status = auto
  branch = auto
  ui = true
[push]
  default = simple
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
  required = true
	process = git-lfs filter-process
